# 背景

我正在将古老人类mtDNA phylotree Build_17的htm文件转为现代的json文件。

# 问题

`data/Arc/mtDNA tree Build 17.htm`是原始文件。
`/mnt/f/OneDrive/文档（科研）/脚本/Download/10-mtDNA/2-单倍群层级划分/data/Haplogrep单倍群分型订正表.tsv`是单倍群规范化表格。

如下2个json文件存在问题：
```sh
/mnt/f/OneDrive/文档（科研）/脚本/Download/10-mtDNA/2-单倍群层级划分/data/phylotree_index_withacc.json.bak
/mnt/f/OneDrive/文档（科研）/脚本/Download/10-mtDNA/2-单倍群层级划分/data/phylotree_index.json.bak
```
同时存在问题的还有：
```sh
/mnt/f/OneDrive/文档（科研）/脚本/Download/10-mtDNA/2-单倍群层级划分/data/phylotree_build17.tsv
```

问题是：这2个json文件以及tsv文件的**部分**的单倍群的上下游关系不正确。
例如：

L5单倍群的上游应该是`L2'3'4'5'6`，`L1`上游的单倍群应该是`L1'2'3'4'5'6`。但是json文件都错了。

# 任务

你的任务是修正json文件中单倍群的上下游关系，使其与原始htm文件中的关系一致。
请注意，修正后的json文件应该保留原有的结构和格式，只修改单倍群的上下游关系部分。你可以使用原始htm文件作为参考，确保修正后的关系正确无误。