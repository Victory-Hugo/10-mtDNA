# 背景

我从公共数据库NCBI下载了一篇文献中的mtDNA数据。这些数据已经从sra格式转为了fastq格式，存放于`/mnt/l/fastq/data`目录下。

然而，当我使用比对方法`/mnt/f/onedrive/文档（科研）/脚本/Download/10-mtDNA/4-Mutect2`将其转为bam→vcf格式时，发现数据质量较差，导致无法进行后续分析。


目前我怀疑是我的脚本存在问题，或者是数据本身质量不佳。

# 需求

现在我需要你任意以一个文件为例，例如以`/mnt/l/fastq/data/SRR36389141.fastq.gz`为例。帮我编写代码（不要删除、修改已有的代码，新的代码放在`/mnt/l/fastq/script/`。编写代码不必遵循pipeline的形式，仅仅快速诊断数据质量问题即可。你可以使用任何你认为合适的工具和方法来进行诊断。

# 软件

请使用`BigLin`的conda环境中的工具进行数据质量诊断。若缺少软件，请优先使用`conda`安装。其次使用`apt`安装。我的sudo密码是`luolintao`。输出的结果放在`/mnt/l/fastq/output`目录下。