# 背景

我已经解决了质控问题。现在所有的脚本都在/mnt/l/fastq/script。

# 要求

现在需要你根据agent skill中的生物信息管线的pipeline skill。对这些脚本进行重构。所有的python需要符合双模块运行。

输出文件的目录要求：

```sh
output/
├── ID
│   ├── 1-XXX/(包括bam文件和配套文件)
│   ├── 2-XXX/(包括vcf文件和配套文件)
│   └── 3-XXX/(包括haplogrep3分型结果（包括fasta文件和extended report的txt文件）和配套文件)
```

配置文件放在`/mnt/l/fastq/conf`目录下。

`conf`需要读取一个`list.tsv`文件，包含样本ID和fastq文件的路径（考虑到这是一个单端测序）。格式如下：

```tsv
ID  fastq_path
sample1  /path/to/sample1.fastq
sample2  /path/to/sample2.fastq
```

请重构脚本，使其能够根据`list.tsv`中的样本ID和fastq路径自动处理每个样本，并将结果输出到对应的目录中。确保脚本具有良好的模块化结构，便于维护和扩展。

你只需要给出脚本，并测试2个文件，不需要全部跑完。