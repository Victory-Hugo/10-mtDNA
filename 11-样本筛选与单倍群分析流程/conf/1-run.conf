# ============================= 用户配置区 ============================= #
# ============================= 以下是必改项目 ============================= #
# 修改这些参数以适应你的数据

# 数据基础目录（必需修改）
BASE_DIR="/mnt/c/Users/yangt/Desktop/样本筛选与单倍群分析流程"

# 输入文件路径（完整的线粒体_现代DNA.xlsx表）
# 可以是绝对路径或相对于BASE_DIR的路径
INPUT_EXCEL="线粒体_现代DNA.xlsx"
# 新样本的Source值（用于标识和筛选新样本）
NEW_SAMPLE_SOURCE=("LLT_20K" "LLT_Hani" "LLT_GSRD_GS" "LLT_GSRD_TB")

# 频率柱状图颜色映射文件
VISUAL_COLOR_CSV="conf/color_LYC.csv"

# 第十步：PCA可视化颜色映射文件
PCA_VIZ_COLOR_CSV="conf/color语系.csv"

# Python环境（如果系统有多个Python版本，可修改）
PYTHON3="/home/jiangyutong/miniconda3/envs/Lin/bin/python3"

# R环境（用于PCA和热图可视化脚本）
RSCRIPT="/home/jiangyutong/miniconda3/envs/Lin/bin/Rscript"


# ============================= 以下根据自己的项目设计修改 ============================= #
# 需要从订正表中提取并合并到基础信息表中的列
# 从标准国家名称表中提取的列名,如：Continent Region等
COUNTRY_EXTRA_COLS=("Continent")
# 从标准民族名称表中提取的列名,如：语系 语言等
ETHNICITY_EXTRA_COLS=("语系" "语言")

# 选择频率分析准备.csv需要的列（这些列会被提取出来进行分析）
SELECTED_COLUMNS=("ID" "Population" "Province" "Country" "Haplogroup_YuChunLi" "Continent" "Language" "Sequence_method" "Classification" "QC_Haplogrep" "Language_family")
# 热图的label按照哪一列来划分？（可选语系、地理位置、省份等等）
HEATMAP_GROUP_COL="Group"
# Classification样本数最小阈值（小于该值的Classification会被排除）
MIN_SAMPLE_COUNT=20

# 单倍群列名（用于计算频率）
HAPLOGROUP_COLUMN="Haplogroup_YuChunLi"

# PCA主成分数
PCA_N_COMPONENTS=10

# PCA可视化时按照哪一列来着色？指定用于大分类（Class_big）的列名,可选值：Group, Continent, Language_family 等
CLASS_BIG_COLUMN="Group"

# 频率堆叠柱状图按照什么分组？
VISUAL_GROUP_COLUMN="Group"

# 是否强制重新运行所有步骤（false=使用缓存跳过已完成步骤，true=重新运行）
FORCE_RUN="true"
