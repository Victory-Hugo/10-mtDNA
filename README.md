# mtDNA 变异综合分析工具库

本仓库提供了整套完整的工具链和脚本，用于人类线粒体 DNA (mtDNA) 的高通量变异检测、质控、单倍群鉴定与群体分析。

## 📋 项目概述

该项目通过整合多个专业化的分析模块，支持从原始测序数据（BAM/CRAM）到最终的遗传学结论的完整分析流程：
- **变异检测**：基于 GATK Mutect2 的高灵敏度 mtDNA 变异发现
- **质量控制**：VerifyBamID2 污染评估和数据完整性验证
- **单倍群分型**：多种单倍群分类方法的支持（Haplogrep3、HaploCart、HaploGrouper）
- **群体分析**：群体遗传学参数计算（Fst、多样性指数、单倍群共享模式等）
- **深度分析**：mtDNA 拷贝数估算、控制区变异、Y 染色体分析

## 📁 目录结构

```
10-mtDNA/
├── 0-提取chrM从BAM/          # 从全基因组 BAM 中提取 chrM 序列
├── 1-单倍群分析/              # 群体单倍群统计与共享模式分析
├── 2-母系专用/               # 母系特有分析（保留用）
├── 3-Haplogrep3变异解读/      # Haplogrep3 注解和解读脚本
├── 4-Mutect2/               # GATK Mutect2 完整变异检测流程
├── 5-多样性和Fst分析/         # 遗传多样性与种群分化分析
├── 6-Y染色体Linux版本/        # Y 染色体分析（限 Linux）
├── 7-单倍群分型/              # 多种单倍群分类工具集成
├── 8-schmutzi/              # 古DNA 污染评估
├── 9-VerifyBamID2/          # BAM 污染与样品混合检测
├── 10-copy_number/          # mtDNA 拷贝数估算
└── README.md                # 本文件

```

## 🔧 核心模块说明

### 0. 提取 chrM 从 BAM
**功能**：使用 samtools 从全基因组 BAM 中提取线粒体序列，生成高效的 chrM BAM 文件。

**依赖**：
- samtools >= 1.10

**脚本**：
- `pipe/1_extract_chrM.sh` - 批量提取 chrM，支持断点续传

**配置**：
- `meta/list.txt` - 样品 ID 和 BAM 路径对照表（TAB 分隔）

---

### 1. 单倍群分析
**功能**：基于群体频率数据，进行单倍群分布、群体共享模式和差异性统计检验。

**依赖**：
- Python 3.6+
- pandas, numpy, scipy, matplotlib, seaborn

**脚本**：
- `python：计算群体共享单倍群.py` - 计算任意两个群体间的单倍群共享百分比
- `python：父系母系共享单倍群.py` - 分父系和母系单倍群进行共享分析
- `python：差异性单倍群统计学检验.py` - 卡方检验等统计方法比较群体差异

---

### 3. Haplogrep3 变异解读
**功能**：使用 Haplogrep3 对 mtDNA 变异进行系统发育和致病性注解。

**依赖**：
- Java >= 11
- Haplogrep3 (需单独下载)

**脚本**：
- `python/1-Haplogrep3-process.py` - 自动化处理和格式转换

**数据**：
- `data/` - Haplogrep 数据库文件

---

### 4. Mutect2 完整流程
**功能**：综合性 mtDNA 变异检测流程，包括非控制区和控制区变异发现、位点过滤和整合。

**依赖**：
- GATK >= 4.2.0
- Picard >= 2.25.0
- BWA >= 0.7.17
- samtools >= 1.10
- bcftools >= 1.10
- GNU parallel (可选，用于并行处理)

**脚本**：
- `pipe/0_准备索引.sh` - 准备参考基因组索引（一次性）
- `pipe/2_Mutect2.sh` - 完整变异检测流程（支持 BAM）
- `pipe/2_Mutect2_CRAM.sh` - CRAM 格式支持版本

**配置**：
- `conf/2_Mutect2.conf` - 参考路径、线程数、内存配置

**工作流程**：
1. 从原始 BAM 提取 chrM 并添加 read group
2. 质量控制和去重
3. 非控制区和控制区分别检测
4. Liftover 到标准参考
5. VCF 合并与最终过滤

---

### 5. 多样性和 Fst 分析
**功能**：计算遗传多样性指标（核苷酸多态性、Watterson θ 等）和群体分化指标（Fst）。

**子目录**：
- `Fst/` - Fst 计算脚本和数据

---

### 6. Y 染色体 Linux 版本
**功能**：Y 染色体单倍群分型（Linux 专用工具）

**要求**：Linux 环境

---

### 7. 单倍群分型
**功能**：整合多种单倍群分类工具。

**工具**：
- `1-HaploCart/` - HaploCart 单倍群分类
- `2-Haplogrep3/` - Haplogrep3 单倍群和致病变异
- `3-HaploGrouper/` - HaploGrouper 系统发育分析

---

### 8. Schmutzi
**功能**：检测和评估古 DNA 中的现代 DNA 污染。

---

### 9. VerifyBamID2
**功能**：基于特异性 SNP 位点，检测 BAM 文件的样品污染和混合。

**依赖**：
- Python >= 3.7
- PyYAML >= 6.0
- VerifyBamID2 (conda 环境)

**脚本**：
- `python/verifybamid2_task.py` - 单样本处理和质控评估
- `python/load_script_config.py` - 配置加载工具
- `script/1-verifybamid2.sh` - 批量处理脚本

**配置**：
- `conf/` - YAML 配置文件（参考路径、conda 环境等）

**核心功能**：
- 运行 VerifyBamID2 混合度检测
- 自动解析结果和质控判断
- 支持断点续传和强制重新运行
- 生成 `.qc.tsv` 结果表单

**使用示例**：
```bash
python verifybamid2_task.py \
  --sample HG001 \
  --bam /path/to/HG001.bam \
  --config conf/verifybamid2.yaml \
  --output-dir output \
  --log-dir log \
  --tmp-dir tmp \
  --force false
```

---

### 10. mtDNA 拷贝数估算
**功能**：基于测序深度，估算线粒体 DNA 的细胞内拷贝数。

**依赖**：
- Python >= 3.7
- PyYAML == 6.0.1
- mosdepth (conda install)

**脚本**：
- `python/mt_copy_number.py` - 核心计算脚本
- `python/config_cli.py` - 配置管理
- `python/merge_results.py` - 结果合并

**配置**：
- `conf/` - 配置文件

---

## ⚙️ 依赖和环境配置

### 系统要求
- Linux/Unix 环境
- Bash >= 4.0
- GNU tools (parallel, xargs 等)

### 软件依赖

#### Python 环境
```bash
pip install PyYAML>=6.0 pandas numpy scipy matplotlib seaborn
```

#### Conda 环境
```bash
# Mutect2 和相关工具
conda create -n gatk-env gatk4=4.2.0.0
conda create -n verifybamid2 -c bioconda verifybamid2
conda create -n mosdepth -c bioconda mosdepth

# 激活使用
conda activate gatk-env
```

#### 核心外部工具
| 工具 | 版本 | 用途 |
|------|------|------|
| samtools | >= 1.10 | BAM 处理、索引 |
| GATK | >= 4.2.0 | 变异检测、VCF 处理 |
| Picard | >= 2.25.0 | BAM 转换和质控 |
| BWA | >= 0.7.17 | 序列比对 |
| bcftools | >= 1.10 | VCF 操作和索引 |
| GNU parallel | >= 20191122 | 并行处理 |
| Haplogrep3 | 最新 | 单倍群分型 |

---

## 🚀 快速入门

### 案例：Mutect2 完整分析流程

1. **准备输入数据**
   ```bash
   # 创建样品列表
   cat meta/list.txt
   # 格式: 样品ID<TAB>BAM路径
   # HG001	/path/to/HG001.bam
   # HG002	/path/to/HG002.bam
   ```

2. **配置环境和参数**
   ```bash
   # 编辑配置文件
   vi conf/2_Mutect2.conf
   
   # 关键参数：
   # - REF_DIR: 参考基因组目录
   # - OUT_DIR: 输出目录
   # - THREADS: 线程数
   # - PARALLEL_JOBS: 并行任务数
   ```

3. **一次性准备索引（首次运行）**
   ```bash
   bash pipe/0_准备索引.sh
   ```

4. **运行变异检测**
   ```bash
   bash pipe/2_Mutect2.sh
   ```

5. **查看结果**
   ```bash
   # VCF 文件位于各样品子目录
   ls output/HG001/*.filtered.vcf.gz
   ls output/HG001/*.qc.tsv  # 质控指标
   ```

### 案例：质量控制 - VerifyBamID2

```bash
# 编辑配置
vi 9-VerifyBamID2/conf/verifybamid2.yaml

# 运行单样本
python 9-VerifyBamID2/python/verifybamid2_task.py \
  --sample HG001 \
  --bam example/HG001.bam \
  --config 9-VerifyBamID2/conf/verifybamid2.yaml \
  --output-dir 9-VerifyBamID2/output \
  --log-dir 9-VerifyBamID2/log \
  --tmp-dir 9-VerifyBamID2/tmp

# 查看结果
cat 9-VerifyBamID2/output/HG001.qc.tsv
```

### 案例：单倍群共享分析

```bash
# 准备频率数据（CSV 格式，样品为列）
python 1-单倍群分析/python：计算群体共享单倍群.py

# 输出：群体间单倍群共享矩阵
```

---

## 📊 输出文件说明

### Mutect2 输出
```
output/样品ID/
├── 样品ID.chrM.filtered.vcf.gz         # 最终变异集合
├── 样品ID.chrM.filtered.vcf.gz.tbi     # VCF 索引
├── 样品ID.chrM.qc.tsv                 # 质控指标
├── 样品ID.chrM.wgs_metrics.txt        # 覆盖深度统计
└── 样品ID.chrM.dedup.metrics           # 去重指标
```

### VerifyBamID2 输出
```
output/
├── 样品ID.qc.tsv                       # 混合度评估 (FREEMIX, PASS/FAIL)
├── 样品ID.selfSM                      # 完整质控报告
└── 样品ID.Ancestry                    # 祖源推断（可选）
```

---

## 📖 使用示例和工作流

### 完整分析流程示例

```bash
# 1. 准备原始 BAM 文件
# 2. 提取 chrM
bash 0-提取chrM从BAM/pipe/1_extract_chrM.sh

# 3. 质量控制
python 9-VerifyBamID2/python/verifybamid2_task.py --sample <ID> ...

# 4. 变异检测
bash 4-Mutect2/pipe/2_Mutect2.sh

# 5. 单倍群分型
# （使用各单倍群分型工具）

# 6. 群体分析
# - Fst 分析
# - 多样性统计
# - 单倍群共享模式
```

---

## 🔍 配置文件说明

### Mutect2 配置示例 (`conf/2_Mutect2.conf`)
```bash
# 参考基因组目录
REF_DIR="/mnt/path/to/reference"

# 输出和临时目录
OUT_DIR="./output"
TMP_DIR="./tmp"
LOG_DIR="./log"

# 计算资源
THREADS=8
PARALLEL_JOBS=4

# GATK 和 Picard 路径（通常通过 conda 管理）
```

### VerifyBamID2 配置示例 (`conf/verifybamid2.yaml`)
```yaml
verifybamid2:
  conda_env: verifybamid2              # conda 环境名
  binary: VerifyBamID                  # 可执行文件路径
  reference: /path/to/reference.fa     # 参考基因组
  svd_prefix: /path/to/svd_prefix      # SVD 面板前缀
  panel_build: GRCh38                  # 参考基因组版本
  reference_build: GRCh38
  num_pc: 2
  num_thread: 4
  disable_sanity_check: false
  output_pileup: false
```

---

## 🧪 测试和调试

### 运行单样本测试
```bash
# 使用示例 BAM 文件
bash 4-Mutect2/pipe/2_Mutect2.sh --sample-id HG001 --bam example/HG001.bam
```

### 查看日志
```bash
# Mutect2 日志
tail -f log/mutect2.HG001.log

# VerifyBamID2 日志
tail -f 9-VerifyBamID2/log/verifybamid2.HG001.log
```

### 断点续传
大多数脚本支持断点续传机制，通过检查 `.success.log` 文件判断已完成样品：
```bash
# 查看已成功处理的样品
cat log/extract_chrM.success.log
cat 9-VerifyBamID2/log/success.log
```

---

## 📝 许可和引用

### 相关文献
- GATK Best Practices for mtDNA analysis
- VerifyBamID2: Zhang et al. (2022)
- Haplogrep3: Kloss-Brandstätter et al.

### 贡献
欢迎提交 Issue 和 Pull Request。

---

## ❓ 常见问题

**Q1: 如何处理 CRAM 格式的输入？**  
A: 使用 `pipe/2_Mutect2_CRAM.sh`，其逻辑与 BAM 版本相同。

**Q2: VerifyBamID2 中的 FREEMIX 值什么时候表示污染？**  
A: 默认阈值为 0.02 (2%)；超过该值判定为 FAIL。可在代码中修改 `FREEMIX_FAIL_THRESHOLD`。

**Q3: 如何并行处理多个样品？**  
A: 脚本默认使用 GNU parallel，通过 `PARALLEL_JOBS` 参数控制并行度。

**Q4: 单倍群分型后的致病变异如何解读？**  
A: 使用 Haplogrep3 的注解功能，结合 mtVar 等数据库查询。

---

## 📞 技术支持

遇到问题？请检查：
1. 依赖软件版本是否满足要求
2. 输入文件格式是否正确
3. 配置文件中的路径是否存在
4. 日志文件中的错误信息
